# Environment variables for URL Shortener

# Server port (Render sets this automatically)
PORT=3001

# MongoDB connection string (use Render's managed MongoDB)
MONGODB_URI=mongodb://username:password@host:port/database

# Redis connection URL (use Render's managed Redis)
REDIS_URL=redis://username:password@host:port

# Redis key for URL ID counter (optional)
REDIS_COUNTER_KEY=url_id_counter